// N ve M ededleri verilir. N ve M nin ededi ortalamasini tapan proqram. (N+M)/2
// Example: 
// M = 10, N= 6 >>>>>>> Output: 8 ( (10+6)/2 )

var numN = prompt ("Enter number N: ");
numNint = Number (numN);

var numM = prompt ("Enter number M: ");
numMint = Number (numM);

var result = (numMint + numNint) / 2;

alert (result);